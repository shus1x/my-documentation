import type MarkdownIt from 'markdown-it';
import { EnabledVariants } from '../common';
export type PluginOptions = {
    runtimeJsPath: string;
    runtimeCssPath: string;
    containerClasses: string;
    bundle: boolean;
    features: {
        enabledVariants: EnabledVariants;
    };
};
type TransformOptions = {
    output?: string;
};
export declare function transform({ runtimeJsPath, runtimeCssPath, containerClasses, bundle, features, }?: Partial<PluginOptions>): (md: MarkdownIt, options?: TransformOptions) => void;
export {};
