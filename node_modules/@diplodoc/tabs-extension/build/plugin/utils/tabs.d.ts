import { RuntimeTab } from '../types';
export declare function getTabId(tab: RuntimeTab, { runId }: {
    runId: string;
}): string;
export declare function isTabSelected(tab: RuntimeTab): boolean;
export declare function getTabKey(tab: RuntimeTab): string;
export declare function getName(tab: RuntimeTab): string;
