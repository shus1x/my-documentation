import Token from 'markdown-it/lib/token';
import { RuntimeTab, TabsProps } from './types';
export declare function props(content: string): TabsProps;
type Result = {
    step: number;
} | {
    content: string;
    closeTokenIndex: number;
};
export declare function tryToFindTabs(tokens: Token[], index: number): Result;
export declare function findTabs(tokens: Token[], idx: number, closeTokenIdx: number): RuntimeTab[];
export {};
